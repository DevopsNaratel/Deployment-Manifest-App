apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: deployment-manifest-apps
  namespace: cicd
spec:
  goTemplate: true
  generators:
  - git:
      repoURL: https://github.com/DevopsNaratel/Deployment-Manifest-App.git
      revision: HEAD
      files:
      - path: "apps/**/values.yaml"
  template:
    metadata:
      name: '{{.path.basename}}'
    spec:
      project: default
      source:
        repoURL: https://github.com/DevopsNaratel/Deployment-Manifest-App.git
        targetRevision: HEAD
        path: base-chart
        helm:
          valueFiles:
            # GUNAKAN ../ karena folder apps ada di luar folder base-chart
            - '../{{.path.path}}/{{.path.filename}}'
      destination:
        server: https://kubernetes.default.svc
        # PERBAIKAN: Langsung panggil .namespace, bukan .values.namespace
        namespace: '{{.namespace}}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
