apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: deployment-manifest-apps
  namespace: cicd
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
  - git:
      repoURL: https://github.com/DevopsNaratel/Deployment-Manifest-App.git
      revision: HEAD
      directories:
      - path: apps/*
  template:
    metadata:
      name: '{{.path.basename}}'
    spec:
      project: default
      source:
        repoURL: https://github.com/DevopsNaratel/Deployment-Manifest-App.git
        targetRevision: HEAD
        path: base-chart
        helm:
          valueFiles:
          - '../{{.path.path}}/values.yaml'
      destination:
        server: https://kubernetes.default.svc
        namespace: '{{.path.basename}}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true